# 沙特 MAZU 极端天气预警项目：三人组统一技术路线 v1

## 一、先用大白话说明我们到底要做什么

老师已经给了我们一套 **2025 年沙特地区的 MAZU 气象指标**。这些数据已经把温度、降水、风、水汽、对流等原始气象数据，整理成了适合风险分析的指标。

我们接下来不是重新研究怎么处理原始气象数据，而是利用老师的数据，先把系统的“后半段”做好：

```text
MAZU指标
→ 风险规则判断
→ 知识图谱补充区域背景、检查证据
→ Agent生成预警报告
```

但老师给的2025指标主要是历史指标，不能直接预测未来。因此，我们还需要用历史天气预报结果，模拟系统在过去某一天真的进行预报：

```text
过去某一天的GraphCast天气预报
→ 转换成与MAZU类似的指标
→ 运行我们用MAZU 2025开发好的风险规则
→ 知识图谱补充宏观区域背景
→ Agent生成当时的预警报告
→ 与后来实际天气和灾害情况比较
```

例如：

```text
2020年某天00时起报
→ GraphCast预测未来24小时
→ 得到预测降水、温度、水汽和风场
→ 转成daily_precip_total、IVT等MAZU-like指标
→ 判断未来24小时哪些地区有暴雨风险
→ Agent生成报告
→ 用IMERG实际降水和灾害记录检查是否报对
```

**第一版不自己运行GraphCast模型，而是直接读取官方已经计算好的GraphCast历史预报。**

这样可以跳过最耗时间的模型部署、全球ERA5输入下载和GPU推理，先把完整流程跑通。未来真正上线时，只需要把“历史GraphCast读取器”替换成MAZU或其他实时预测模型，后面的指标、规则、知识图谱和Agent都可以继续使用。

---

# 二、第一版项目只做什么

为了保证三个人能够真正完成，第一版只做两个天气风险：

1. **未来24/48/72小时强降水风险**
2. **未来24/48/72小时高温和热浪风险**

暂时不正式做：

* 沙尘暴风险：缺少沙尘浓度、AOD和能见度；
* 完整山洪灾害风险：缺少流域、wadi、土壤和详细承灾体数据；
* 复杂机器学习风险模型；
* 自己运行完整GraphCast；
* 全部91个MAZU指标复刻。

强降水之后可以输出“山洪气象潜势”，但第一版不能直接声称预测了真实山洪。

---

# 三、需要的数据：只下载最必要的部分

## 1. 老师给的MAZU 2025指标

### 状态

已经有，不需要重新下载。

### 用途

它不作为最终测试答案，而是用来：

* 理解指标定义；
* 选取风险规则需要的指标；
* 查看2025年各指标分布；
* 确定阈值和权重；
* 测试规则代码；
* 设计知识图谱结构；
* 设计Agent报告格式。

老师的指标覆盖2025年365个日文件、约0.1°网格，包含降水、温度、CAPE、PWAT、IVT、风场、海温、热浪和山洪初筛指标；但不同指标有效天数不同，DS8距平和DS10短时降水也存在明确限制。

---

## 2. GraphCast 2020历史预报

### 为什么用它

WeatherBench 2已经公开了GraphCast的2018年和2020年历史预报。2020年预报由训练截至2019年的模型生成，并使用ERA5初始化，因此2020年可以作为相对独立的历史回报测试。数据空间分辨率为0.25°、时间步长为6小时、最长提前量为10天。([WeatherBench 2][1])

### 获取地址

说明页面：

[WeatherBench 2 Data Guide](https://weatherbench2.readthedocs.io/en/latest/data-guide.html)

数据目录：

```text
gs://weatherbench2/datasets/graphcast/
```

2020年数据：

```text
gs://weatherbench2/datasets/graphcast/2020/
date_range_2019-11-16_2021-02-01_12_hours_derived.zarr
```

官方数据中包含00 UTC和12 UTC起报、6小时至10天的预报，并包含2米温度、10米风、6小时和24小时降水，以及多层比湿、风、位势和垂直速度。([WeatherBench 2][1])

### 不要下载整个数据集

整个全球数据非常大。正确做法是通过`xarray + gcsfs`远程读取，只保存：

* 选中的事件日期；
* 沙特及周边区域；
* 必要变量；
* 24、48和72小时预报需要的时次。

### 获取范围

```text
纬度：15°N—33°N
经度：33°E—57°E
```

比老师的bbox稍微扩大一圈，避免边界地区被截掉。进入风险分析前再使用沙特国界掩膜。

### 获取时间

第一版只获取2020年选定的测试事件，不下载全年。

建议测试样本：

* 10个强降水过程；
* 10个高温过程；
* 20个普通天气过程；
* 共约40个测试窗口。

### 获取提前量

每个起报时间只读取：

```text
+6、+12、+18、+24小时
+30、+36、+42、+48小时
+54、+60、+66、+72小时
```

这样可以分别生成未来0—24、24—48和48—72小时指标。

### 只获取这些变量

| GraphCast变量               | 用途            |
| ------------------------- | ------------- |
| `total_precipitation_6hr` | 计算各24小时窗口累计降水 |
| `2m_temperature`          | 日平均、最高和最低气温   |
| `10m_u_component_of_wind` | 计算10米风速       |
| `10m_v_component_of_wind` | 计算10米风速       |
| `specific_humidity`       | 计算PWAT、IVT    |
| `u_component_of_wind`     | IVT、850风、风切变  |
| `v_component_of_wind`     | IVT、850风、风切变  |
| `vertical_velocity`       | 500 hPa上升运动   |
| `geopotential`            | 500 hPa位势高度   |

气压层只保存：

```text
1000、925、850、700、500、300、200 hPa
```

第一版不下载其他变量和层次。

### 读取示例

```python
import xarray as xr

path = (
    "gs://weatherbench2/datasets/graphcast/2020/"
    "date_range_2019-11-16_2021-02-01_12_hours_derived.zarr"
)

ds = xr.open_zarr(
    path,
    storage_options={"token": "anon"},
)

variables = [
    "total_precipitation_6hr",
    "2m_temperature",
    "10m_u_component_of_wind",
    "10m_v_component_of_wind",
    "specific_humidity",
    "u_component_of_wind",
    "v_component_of_wind",
    "vertical_velocity",
    "geopotential",
]

subset = ds[variables].sel(
    time="2020-08-20T00:00:00",
    prediction_timedelta=[
        "6h", "12h", "18h", "24h",
        "30h", "36h", "42h", "48h",
        "54h", "60h", "66h", "72h",
    ],
    lat=slice(15, 33),
    lon=slice(33, 57),
    level=[1000, 925, 850, 700, 500, 300, 200],
)

subset.to_netcdf("graphcast_case.nc")
```

实际运行时应先测试变量名称和维度，再批量处理。

---

## 3. IMERG Final V07实际降水

### 用途

IMERG是强降水评估的主要“参考答案”，用于检查：

* 实际是否下了强雨；
* 实际降水时间；
* 实际降水地点；
* 实际24小时降水；
* 实际最大1小时、3小时和6小时降水。

IMERG Final V07提供0.1°、30分钟降水，2020年数据完整可用。Final产品包含月尺度雨量计订正，历史数据从2000年开始。([NASA Earthdata][2])

### 获取地址

产品页面：

[IMERG Final Half-Hourly V07](https://disc.gsfc.nasa.gov/datasets/GPM_3IMERGHH_07/summary)

NASA Earthdata目录：

[Earthdata IMERG目录](https://www.earthdata.nasa.gov/data/catalog/ges-disc-gpm-3imerghh-07)

需要免费注册NASA Earthdata账号。

### 下载范围

```text
纬度：15°N—33°N
经度：33°E—57°E
```

### 下载时间

只下载40个测试过程的有效时间，并在事件前后各多下载6小时。

例如预报有效期：

```text
2020-08-20 00 UTC—2020-08-21 00 UTC
```

IMERG下载范围可以设为：

```text
2020-08-19 18 UTC—2020-08-21 06 UTC
```

### 下载变量

第一版只需要：

```text
precipitation
```

可选质量变量：

```text
precipitationQualityIndex
```

不要下载所有中间变量，也不要下载全球完整文件。

### 时间和空间处理

* 30分钟数据累加成1、3、6和24小时降水；
* IMERG为0.1°；
* GraphCast为0.25°；
* 评估时将IMERG聚合到GraphCast的0.25°网格；
* 不建议把GraphCast强行放大成0.1°后再声称具有0.1°预测能力。

---

## 4. NOAA GHCN-Daily地面站数据

### 用途

主要用于验证：

* 日最高气温；
* 日最低气温；
* 日累计降水；
* 热浪持续时间。

GHCN-Daily是经过质量控制的全球日值站点数据，包含最高温、最低温和日降水等变量。([国家环境信息中心][3])

### 获取地址

产品说明：

[GHCN-Daily](https://www.ncei.noaa.gov/products/land-based-station/global-historical-climatology-network-daily)

在线检索：

[NOAA Climate Data Online](https://www.ncei.noaa.gov/cdo-web/search?datasetid=GHCND)

直接文件目录：

```text
https://www.ncei.noaa.gov/pub/data/ghcn/daily/
```

### 获取范围和时间

* 国家：Saudi Arabia；
* 年份：2020；
* 变量：`TMAX`、`TMIN`、`PRCP`；
* 下载沙特境内所有有数据的站点即可。

站点文件很小，可以直接下载2020全年，不必只下载事件日。

### 使用要求

* 保留质量标志；
* 优先使用测试期间数据完整的站点；
* 网格预测值与最近站点比较；
* 不把某个站点代表整个省份。

第一版不必额外下载小时站点数据；需要检验短时温度和风时，再增加ISD/GHCNh。NOAA的ISD包含温度、露点、风、气压、能见度和不同时间累计降水，但不是第一版必需。([国家环境信息中心][4])

---

## 5. 沙特国界和一级行政区

### 用途

用于：

* 去掉bbox中的海洋和邻国；
* 把网格风险聚合到省级区域；
* 让知识图谱以宏观行政区为单位表达风险；
* 统计各省风险面积和最大指标。

### 获取地址

国家边界ADM0：

```text
https://www.geoboundaries.org/api/current/gbOpen/SAU/ADM0/
```

一级行政区ADM1：

```text
https://www.geoboundaries.org/api/current/gbOpen/SAU/ADM1/
```

API返回的JSON中包含GeoJSON下载地址。`gbOpen`采用CC BY 4.0，使用时注明来源即可。([geoboundaries.org][5])

第一版只下载ADM0和ADM1，不下载更细的ADM2、道路、建筑和人口数据。

---

## 6. 灾害和影响记录

这一部分不用下载巨大数据集，而是人工建立一个小型事件表。

### 数据来源

优先顺序：

1. 沙特政府、气象和民防公开报告；
2. [ReliefWeb](https://reliefweb.int/)；
3. [EM-DAT](https://public.emdat.be/)；
4. 可靠新闻作为补充。

ReliefWeb由联合国OCHA提供，适合搜索灾害报告和官方通报；EM-DAT适合确认重大灾害，非商业用途注册后可以免费访问，但对小范围局地积水可能没有记录。([人道主义信息网][6])

### 事件表格式

```csv
event_id,hazard,start_time,end_time,region,city,
impact_status,impact_description,source_1,source_2,confidence
```

其中：

```text
impact_status:
yes      明确有灾害影响
no       明确报告未造成明显影响
unknown  没找到足够资料
```

非常重要：

> 没有查到新闻，不能直接等于“没有灾害”，应标记为unknown。

---

# 四、第一版明确不需要下载的数据

为了控制工作量，以下数据暂时不下载：

| 数据            | 暂不使用原因                        |
| ------------- | ----------------------------- |
| SaRa          | 当前无法获取                        |
| ERA5全球数据      | 第一版直接使用预计算GraphCast，不自己运行模型   |
| OISST/GHRSST  | 第一版风险规则不依赖SST                 |
| MERRA-2       | 暂不做正式沙尘模块                     |
| CHIRPS        | 不做长期干旱分析                      |
| SRTM          | 老师的MAZU数据已有`orography`，可先聚合使用 |
| OpenStreetMap | 第一版知识图谱不存具体道路和建筑              |
| WorldPop      | 第一版先评估天气和宏观风险，不做精细人口损失        |

等第一版完成后再按需要增加，不要一开始把所有数据都下载下来。

---

# 五、怎么利用MAZU 2025进行开发

## 1. 先找“MAZU和GraphCast共有的指标”

不是老师文件里的所有指标都能由GraphCast生成。

第一版统一使用以下指标：

| MAZU-like指标              | GraphCast生成方法    |
| ------------------------ | ---------------- |
| `daily_precip_total`     | 连续4个6小时降水相加      |
| `t2m_c`                  | 4个6小时2米温度平均并转摄氏度 |
| `tmax_c`                 | 4个时次最大值          |
| `tmin_c`                 | 4个时次最小值          |
| `wind10_speed`           | 10米U/V风合成        |
| `pwat`                   | 比湿按气压垂直积分        |
| `ivt`                    | 比湿乘风场后按气压积分      |
| `wind850_speed`          | 850 hPa U/V风合成   |
| `wind_shear_850_200`     | 850与200 hPa风矢量差  |
| `omega500`               | 500 hPa垂直速度      |
| `geopotential_height500` | 500 hPa位势除以重力加速度 |
| `heatwave_day_flag`      | 使用最高温规则计算        |
| `heatwave_duration_days` | 连续热浪日计数          |

第一版暂不使用：

* `daily_convective_precip`
* `daily_large_scale_precip`
* `cape`
* `heat_index_c`
* `vpd_kpa`
* `sst_celsius`
* DS10短时降水
* 原来的`flash_flood_risk`

原因不是这些指标没用，而是GraphCast公开历史输出不能稳定地复刻全部指标。

---

## 2. 用MAZU 2025确定指标范围和风险规则

### 强降水规则

先从2025年MAZU数据中，按沙特陆地区域统计：

* `daily_precip_total`
* `pwat`
* `ivt`
* `wind850_speed`
* `omega500`

计算：

* 全年分布；
* 各季节分布；
* 各省P90和P95；
* 典型高值事件。

第一版规则可以采用：

```text
预测降水达到绝对阈值
+ PWAT达到当地高值
+ IVT达到当地高值
+ 500 hPa存在上升运动
→ 强降水风险升高
```

建议风险评分：

```text
预测24小时降水达到阈值：权重最高
IVT达到区域季节P90：加分
PWAT达到区域季节P90：加分
omega500明显为负：加分
850 hPa风较强：加分
```

不要一开始人工写很多复杂规则，先做一套可解释的评分规则。

### 高温规则

优先沿用老师现有逻辑：

```text
tmax_c ≥ max(40°C, 当地同期气候态最高温 + 5°C)
→ heatwave_day_flag = 1
```

连续3天或以上：

```text
→ 热浪风险增强
```

同时参考`tmin_c`，判断夜间是否仍然高温。老师现有热浪日和持续天数指标已经按照绝对高温与相对气候态相结合的方式计算。

---

## 3. 在2025年数据上做“开发测试”

使用2025年数据完成：

* 指标读取是否正确；
* 各指标单位是否正确；
* 风险规则能否运行；
* 典型事件是否能被找出来；
* 风险结果是否能聚合到行政区；
* 图谱能否解释；
* Agent报告是否与数据一致。

这一步只能叫：

```text
规则开发和内部一致性测试
```

不能叫最终准确率评估。

---

## 4. 冻结规则

完成2025年开发后，固定：

```text
规则条件
阈值
权重
风险等级
缺测处理方法
Agent报告模板
```

保存为：

```text
heavy_rain_rules_v1.yaml
heatwave_rules_v1.yaml
```

然后再跑2020年测试。

看到2020结果以后，不能一边测试一边继续修改规则。修改后，2020就不再是独立测试集。

---

# 六、MAZU-like输出文件如何设计

不需要把GraphCast伪装成真正的0.1°模型。

建议保持GraphCast原生0.25°，但保证：

* 指标名称一致；
* 单位一致；
* 公式一致；
* 时间含义一致；
* 缺测处理一致；
* 文件字段规范一致。

文件名：

```text
mazu_like_20200820_00_lead024.nc
mazu_like_20200820_00_lead048.nc
mazu_like_20200820_00_lead072.nc
```

必须保存的元数据：

```text
forecast_model = GraphCast
initial_time = 2020-08-20T00:00:00Z
lead_time_hours = 24
valid_start_time = 2020-08-20T00:00:00Z
valid_end_time = 2020-08-21T00:00:00Z
source_resolution = 0.25 degree
indicator_version = mazu_like_v1
```

完整网格仍存在NetCDF中；风险结果再聚合到沙特ADM1。

---

# 七、知识图谱怎么设计

## 1. 图谱只做宏观表达

知识图谱不保存每个网格、每个小时的气象数值。

完整数值保存在NetCDF，图谱只保存：

* 哪次预报；
* 哪个省或城市；
* 哪类风险；
* 风险等级；
* 哪些指标支持；
* 哪些证据缺失；
* 采用了哪条规则；
* 实际观测是否支持；
* 是否发现灾害影响。

## 2. 第一版节点

```text
Region
ForecastCase
Hazard
RiskAssessment
IndicatorSummary
Rule
Evidence
DataSource
```

## 3. 主要关系

```text
ForecastCase  → occursIn       → Region
ForecastCase  → hasAssessment  → RiskAssessment
RiskAssessment → concerns      → Hazard
RiskAssessment → supportedBy   → IndicatorSummary
RiskAssessment → evaluatedBy   → Rule
RiskAssessment → contradictedBy→ Evidence
Evidence       → derivedFrom   → DataSource
```

## 4. 图谱中保存什么指标

例如某次Makkah省未来24小时预报，只保存：

```text
区域最大24小时降水：42 mm
区域平均降水：8 mm
超过20 mm的面积比例：18%
区域最大IVT：360 kg m-1 s-1
高PWAT面积比例：25%
风险等级：高
```

不保存数万个网格节点。

## 5. 技术选择

第一版直接使用：

```text
Neo4j Community Edition
```

风险规则在Python/YAML中执行，知识图谱不负责复杂数值计算。

Agent读取的是：

```text
风险结果JSON
+ 知识图谱区域背景
+ 观测和灾害证据
```

然后生成报告。

---

# 八、最终评估怎么做

评估分两层。

## 第一层：天气预测是否正确

### 强降水

GraphCast预测与IMERG比较：

* 是否预测到强降水；
* 时间是否正确；
* 地点是否正确；
* 预测降水量误差；
* 风险区域与实际降水区域重叠程度。

### 高温

GraphCast预测与GHCN-Daily站点比较：

* 最高温误差；
* 最低温误差；
* 高温日是否命中；
* 热浪开始时间；
* 热浪持续天数。

## 第二层：灾害风险是否正确

与事件真值表比较：

| 情况   | 含义            |
| ---- | ------------- |
| 命中   | 报警，实际也发生      |
| 漏报   | 没报警，但实际发生     |
| 空报   | 报警，但明确没有发生    |
| 未知   | 报警，但找不到足够灾情资料 |
| 地点误差 | 报对灾种，但区域不对    |
| 时间误差 | 报对灾种和区域，但时间不对 |

天气预测正确但没有灾害，不一定说明天气模型错误；可能是当地没有形成影响。因此天气和灾害必须分开统计。

---

# 九、三个人怎么分工

## 成员A：预测数据与MAZU-like转换

### 主要任务

1. 远程读取GraphCast 2020数据；
2. 按事件日期、区域、变量和提前量裁剪；
3. 计算MAZU-like指标；
4. 统一单位和时间窗口；
5. 输出NetCDF；
6. 编写批处理脚本。

### 交付物

```text
graphcast_loader.py
indicator_converter.py
indicator_mapping.yaml
mazu_like_*.nc
data_contract.md
```

### 第一周要完成

* 成功读取一个GraphCast日期；
* 裁剪沙特区域；
* 生成一个24小时`daily_precip_total`；
* 生成`tmax_c`、`ivt`和`wind850_speed`；
* 输出一个样例NC文件。

---

## 成员B：MAZU 2025分析、风险规则和评估

### 主要任务

1. 分析老师2025指标的分布；
2. 计算分季节、分区域P90/P95；
3. 编写强降水和高温规则；
4. 实现风险评分引擎；
5. 输出风险等级和支持证据；
6. 编写命中、漏报、空报和误差统计。

### 交付物

```text
mazu_2025_analysis.ipynb
heavy_rain_rules_v1.yaml
heatwave_rules_v1.yaml
risk_engine.py
risk_result_*.json
evaluation.py
metrics.csv
```

### 第一周要完成

* 确定第一版共同指标；
* 完成2025年这些指标的统计；
* 写出第一版规则；
* 用老师的一天数据跑出风险JSON。

---

## 成员C：真实数据、知识图谱和Agent

### 主要任务

1. 下载IMERG测试事件数据；
2. 下载GHCN-Daily站点数据；
3. 建立2020事件和普通日期清单；
4. 人工整理灾害事件真值表；
5. 下载ADM0/ADM1边界；
6. 设计并搭建Neo4j图谱；
7. 把风险JSON写入图谱；
8. 让Agent生成报告。

### 交付物

```text
event_catalog.csv
imerg_observation_*.nc
ghcn_2020.csv
saudi_adm1.geojson
neo4j_schema.cypher
graph_import.py
agent_report.py
report_*.md
```

### 第一周要完成

* 找到一个2020年强降水事件；
* 下载对应IMERG数据；
* 建立事件表模板；
* 下载沙特ADM1；
* 搭建最小Neo4j图谱；
* 手工导入一个事件。

---

# 十、三个人的接口必须提前统一

成员A输出：

```text
mazu_like_case.nc
```

成员B读取NC，输出：

```json
{
  "case_id": "20200820_00_024",
  "region": "Makkah",
  "hazard": "heavy_rain",
  "risk_level": "high",
  "risk_score": 7,
  "triggered_conditions": [
    "daily_precip_total >= 20 mm",
    "ivt >= regional_p90"
  ],
  "missing_conditions": [],
  "source_file": "mazu_like_20200820_00_lead024.nc"
}
```

成员C读取风险JSON，结合：

```text
行政区
IMERG观测
站点观测
灾害记录
```

写入知识图谱并生成报告。

这样三个人可以同步开发，不需要等其中一个人全部做完。

---

# 十一、建议四周实施计划

| 周次  | 成员A           | 成员B         | 成员C            | 共同目标     |
| --- | ------------- | ----------- | -------------- | -------- |
| 第1周 | GraphCast样例读取 | MAZU统计和规则草案 | IMERG、事件表、图谱样例 | 打通一个数据样例 |
| 第2周 | MAZU-like转换脚本 | 风险引擎        | 图谱导入和Agent模板   | 跑通一个完整事件 |
| 第3周 | 批量处理40个窗口     | 批量风险判断      | 观测与灾情匹配        | 完成批量测试   |
| 第4周 | 修复数据问题        | 计算评估指标      | 完成图谱与报告        | 形成最终结果   |

---

# 十二、第一阶段完成的标准

第一阶段不用追求大而全，只要做到：

```text
读取一个2020年GraphCast历史预报
→ 转成MAZU-like指标
→ 风险规则判断
→ 聚合到沙特行政区
→ 写入知识图谱
→ Agent生成报告
→ IMERG和站点验证
→ 给出命中/漏报/空报结果
```

最终系统定位是：

> **MAZU 2025负责开发指标接口和风险规则；GraphCast 2020负责历史回报；IMERG与GHCN负责天气验证；灾害报告负责影响验证；知识图谱负责宏观地区、规则和证据组织；Agent负责生成可追溯的预警报告。**

[1]: https://weatherbench2.readthedocs.io/en/latest/data-guide.html "WeatherBench 2 Data Guide — WeatherBench 2  documentation"
[2]: https://www.earthdata.nasa.gov/data/catalog/ges-disc-gpm-3imerghh-07?utm_source=chatgpt.com "GPM IMERG Final Precipitation L3 Half Hourly 0.1 degree ..."
[3]: https://www.ncei.noaa.gov/products/land-based-station/global-historical-climatology-network-daily "Global Historical Climatology Network daily (GHCNd) | National Centers for Environmental Information (NCEI)"
[4]: https://www.ncei.noaa.gov/products/land-based-station/integrated-surface-database "Global Hourly - Integrated Surface Database (ISD) | National Centers for Environmental Information (NCEI)"
[5]: https://www.geoboundaries.org/api.html "geoBoundaries"
[6]: https://reliefweb.int/?utm_source=chatgpt.com "ReliefWeb - Informing humanitarians worldwide"
