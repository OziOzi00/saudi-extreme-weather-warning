# 2026-07-16 案例扩展与观测可用性核验

本记录说明在不区分成员执行人的前提下，本轮实际推进的案例、证据和观测工作。所有状态均以仓库文件和本机可复验结果为准。本文创建后的后续状态是：成员 A 已按单人推进决定批准并冻结 12 个真实案例，GraphCast 正式批处理已于 2026-07-16 启动；详见[阶段进展与正式批处理启动](2026-07-16_阶段进展与正式批处理启动.md)。

## 本轮完成

1. 将 `configs/case_catalog_candidates.csv` 扩展为 13 行：7 个事件候选、5 个对照候选和 1 个流程 demo。
2. 为候选案例增加 `dataset_split`，区分 `proposed_development`、`proposed_independent_test` 和 `demo`。当前划分只是预注册草案，不是最终冻结划分。
3. 扩展可追溯来源和影响记录：`source_catalog.csv` 共 14 条来源，`disaster_impact_truth.csv` 共 12 条证据记录；这些记录随后已于 2026-07-16 完成逐条复核，仍没有把“未找到影响”写成 `no`。
4. 下载并校验 NOAA GHCN-Daily 2020 全球年文件和站点表，在不提交原始大文件的前提下筛选沙特站点与候选窗口。
5. 下载并校验 22 个 NASA IMERG Final Run V07B 日累计文件，按官方缩放/缺测规则完成沙特 ADM1 聚合和暴雨对照筛选。
6. 更新候选案例 Schema、知识图谱 bundle 校验和回归测试；重新生成的开发 bundle 为 55 个节点、56 条关系。

## 候选案例与划分草案

| 类别 | 数量 | 用途 |
| --- | ---: | --- |
| 事件候选 | 7 | 暴雨/洪涝或高温过程，等待证据复核和观测筛选 |
| 对照候选 | 5 | 已由 IMERG 或 GHCN 确认相对同区域事件为低强度；仍待影响资料复核 |
| 流程 demo | 1 | 仅复验 GraphCast → MAZU-like 工程流程 |

独立测试候选在规则冻结前不得用于调阈值。只有在来源复核、观测覆盖检查和全队确认后，才能把 `proposed_*` 改为正式 `development` 或 `independent_test`。

## GHCN 实际可用性

本机原始文件位于被 Git 忽略的 `data/external/ghcn_daily/`：

| 文件 | 大小（字节） | SHA-256 |
| --- | ---: | --- |
| `2020.csv.gz` | 171044088 | `f9056cc20dcffdc4d3227074d364295648ba502e62d5b2b3d1f7f75d2991e433` |
| `ghcnd-stations.txt` | 11395053 | `fedc8bbe92f9b1eee0a57f8886e5e022965a542efa60710c01b47a838120b18d` |

筛选脚本识别出 32 个落在固定沙特 ADM1 边界内的站点。版本化摘要见：

- `manifests/ghcn_2020_candidate_coverage.csv`
- `manifests/ghcn_2020_saudi_daily_summary.csv`
- `manifests/observation_source_availability.csv`

初步结果显示，2020 年 7 月末高温候选在 SA-08 和 SA-04 的 GHCN `TMAX` 分别达到 50.0°C 和 50.6°C。西南部 7 月洪涝候选窗口有少量 PRCP 站点记录，但多数降水候选/对照窗口没有足够 GHCN PRCP 覆盖。因此 GHCN 可作为温度主验证和降水辅助证据，区域降水仍需要 IMERG。

## IMERG 状态

- 官方产品页：<https://gpm.nasa.gov/data/imerg>
- 历史回放拟固定使用 Final Run V07B；不得与 Early/Late Run 或其他版本混用。
- 产品在沙特区域及 2020 年时间范围内可用，空间分辨率约 0.1°、时间分辨率半小时。
- 当前 22 个日文件已全部下载并校验，总计约 251.7 MiB；逐文件 SHA-256 见 `manifests/imerg_v07b_daily_files.csv`。
- `scripts/screen_imerg_candidates.py` 按官方 0.1 mm 缩放和 `29999` 缺测值处理 GeoTIFF，并生成逐日、案例窗口和对照筛选摘要。
- 4 个暴雨对照在目标区域的窗口 P95 和最大值均低于 31 天内匹配事件；与 GHCN 高温对照一起，5 个对照均已获得相对低强度天气证据。

## 当前闸门与下一步

观测筛选和首轮证据复核闸门已经通过。成员 A 随后在单人继续推进的工作方式下确认批准，候选状态和数据集划分已冻结。下一步应：

1. 按已冻结的 development/independent-test 划分运行 GraphCast 批处理；
2. 对媒体证据继续寻找官方原文或第二独立来源，但不以此虚构更高的证据等级；
3. 再由 A 的既有批处理生成相应 24/48/72h MAZU-like NetCDF；
4. 把正式预报与现有 IMERG/GHCN 摘要配对，进入独立效果评估。

这样可以避免先花时间下载和计算 GraphCast，最后才发现案例、对照或独立测试划分不合格。
