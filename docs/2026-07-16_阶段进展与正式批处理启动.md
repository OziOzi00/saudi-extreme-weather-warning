# 2026-07-16 阶段进展与正式批处理启动

本文最初记录正式批处理启动，现已更新至2026-07-17的A侧交付完成状态。任务仍按A/B/C的技术职责归属记录；由于项目已决定允许一人按依赖顺序连续推进，B、C名下的部分产物实际由成员A代为实现。文中的“已完成”表示仓库产物可复验，不表示B、C本人已经分别验收。

## 当前结论

- 12 个真实案例已经批准：7 个 `development`、5 个 `independent_test`；另有 1 个 `demo` 不参加效果评价。
- GHCN 2020 和 IMERG Final Run V07B 的最小观测准备、QC、ADM1 聚合及候选筛选已经完成。
- 12 条灾害影响真值已经逐条复核：9 条保持 `yes`，3 条高温影响保持 `unknown`。
- GraphCast → MAZU-like正式批处理已完成：12个真实案例、144个时次、36份正式NetCDF全部生成；加上demo共39份文件均通过验收。
- ADM1区域摘要已重建为5577行；delivery manifest共39行，全部`passed`并记录文件SHA-256、实现指纹和Git revision。
- 全仓库回归为 `56 passed`；知识图谱开发 bundle 为 55 个节点、56 条关系。

## 成员 A 归属

### 已完成

1. GraphCast 2020 远程读取、沙特裁剪、+6 至 +72 小时可恢复缓存。
2. 11 个 MAZU-like 指标及 24/48/72 小时 NetCDF 生成。
3. catalog 预检查、NetCDF QC、原子交付、损坏缓存隔离、SHA-256 溯源和一键交付。
4. `20200820_00` demo 的三个 lead 全链路复验。
5. 接收已批准 catalog，并启动 12 个真实案例的正式批处理。

### 交付结果与后续支持

1. 12个案例的全部缓存、36份MAZU-like交接文件和processing manifest均已完成。
2. 39份文件均通过NetCDF契约、时间连续性和交付验收。
3. ADM1摘要和delivery manifest已重建，可正式交给B；A后续只处理可复现的接口问题。

## 成员 B 归属（本轮由 A 代办）

### 已完成

1. MAZU 2025 的 11 字段覆盖审计及 ADM1 年度/季节描述统计。
2. MAZU/GraphCast 时间语义、候选阈值解释、暴雨和热浪 `draft` 规则。
3. 草案风险引擎、78 份隔离试跑结果及 Risk JSON 验收器。
4. GHCN 2020 下载、散列与沙特候选窗口筛选。
5. 28个IMERG Final V07B日文件的认证下载、ZIP/SHA校验、官方缩放/缺测处理和ADM1聚合；其中22个用于案例窗口筛选，另6个补齐development lead配对。
6. 五个对照均获得相对低强度天气证据；该结论不等于“无灾害影响”。

### 剩余

1. 等待 A 正式批处理完成，建立逐案例、逐区域、逐 lead 的预报—观测配对。
2. 完成降水误差、CSI、命中/漏报/空报、空间偏差，以及温度 MAE/RMSE 和热浪时序验证。
3. 结合 development 结果复核规则；不得查看 independent_test 结果调阈值。
4. 冻结规则并生成通过正式验收的 Risk JSON。

## 成员 C 归属（本轮由 A 代办）

### 已完成

1. 13 个 ADM1 的统一 `region_id`、阿拉伯语名称、行政中心与来源记录。
2. 7 个事件、5 个对照和 1 个 demo 的案例目录、Schema 与批准记录。
3. 14 条来源目录、12 条逐证据影响真值及复核 manifest；没有把缺少报道写成 `no`。
4. 天气层与影响层拆分：12 个真实案例进入天气层，只有 5 个有已复核影响证据的暴雨事件进入影响层正例。
5. Neo4j 5.x 约束、开发 bundle、导入器、固定查询和受控报告模板。

### 剩余

1. 评估更近且许可清晰的官方边界；未取得前继续标注当前 2017 参考边界。
2. 在真实 Neo4j Community 服务上执行约束、导入和固定查询。
3. 接收冻结 Risk JSON，生成正式 bundle 与报告。
4. 完成灾害影响层命中、漏报、空报分析，`unknown` 不得作为负例。

## 后续严格顺序

```text
A 完成 GraphCast 批处理、QC 和交付
  → B 完成 development 天气验证并冻结规则
  → B 在 independent_test 上只做一次最终评估并输出正式 Risk JSON
  → C 实机导入图谱、生成正式报告并完成影响层验证
  → 全项目复盘并形成最终状态说明
```

GraphCast、IMERG、GHCN 和 MAZU 原始大文件继续保留在本地忽略目录；GitHub 只提交代码、配置、Schema、摘要、散列、可控示例和状态记录。
