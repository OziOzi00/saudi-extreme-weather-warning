# Changelog

- 2026-07-19完成整体规则与图谱纠错统一基准：汇总16个已开放高温development案例、37个目标日，比较12组订正/聚合候选，无方案通过召回、特异度、事件检出和对照拒绝全部门槛，独立高温保持封存；暴雨v2保持唯一正式规则。比较5组预测态图谱候选后选择`ratio>=0.50 + support>=2`作为未激活影子方案，development由3/5改善为4/5且4/4对照正确否定，但正确增益仅来自1个事件案例；新增`agent_forecast_report_v3`把基础风险与影子建议分字段保存，禁止覆盖Risk JSON或读取同期真值，旧v2 Schema继续保留。

## v0.1.0-prototype — 2026-07-17

- 将现有 GraphCast → MAZU-like → 冻结暴雨风险 → 图谱/报告成果收束为稳定研究原型，不修改冻结规则或既有独立评估结果。
- 新增无需原始数据和 Neo4j 服务的 `scripts/run_prototype_demo.ps1`，统一验收51份NetCDF交付记录、153条development配对和33份冻结暴雨Risk JSON。
- 新增确定性的 `handoff/prototype_demo/demo_summary.json`、发布边界测试、正向覆盖案例与已知漏报案例，明确高温阻塞、影响负例缺失和非生产部署边界。
- 完整回归测试更新为98项通过。

## Unreleased — documentation and collaboration contract upgrade

- 2026-07-19在预注册锁提交并推送后完成GraphCast 2018六案例跨年前瞻批次：72/72缓存、18/18 MAZU-like NetCDF及2574行ADM1指标摘要闭合。唯一`lead_specific_median`候选的观测高温日召回为5/6，但非高温日特异度为8/12、对照案例拒绝为2/3，未通过全部门槛；未事后搜索阈值或最大值权重，高温继续blocked，独立高温继续封存。
- 2026-07-18扩展并校验2018年NOAA SSODv2：33个站点原文件保持Git忽略，32站分配至ADM1并生成26,238行区域日摘要；只凭观测锁定3个高温事件和3个同区域对照，固定唯一`lead_specific_median`候选、输入SHA和成功门槛。锁定时6个起报时间均无GraphCast/MAZU-like产物，独立高温未打开。
- 2026-07-18接入预测前静态知识上下文：固定WorldClim 2.1高程与1970–2000月降水基线的URL、SHA、保守available_at和10角分辨率限制；生成13 ADM1 × 12个月共156个面积加权画像，强制`context_only`与空知识风险。
- 2026-07-18完成development-only上下文审计：9/9暴雨目标窗具备静态背景且0次改变风险；独立集未读取。现有一致性关注从3命中/2漏报/0误报变为5命中/0漏报/3误关注，决定不升级为纠错规则。
- 2026-07-18将未通过门槛的一致性候选降级为`development_gate_failed_diagnostic_only`：继续输出`possible_underestimation`供审计，但取消候选watch对有效attention的影响，默认低风险报告恢复routine。
- 2026-07-18在查看细空间指标前预注册P99/最大值/超阈面积热点条件，随后从15份development MAZU-like网格提取局地极值、面积、集中度和地形关系。候选触发0次、漏报减少0，未达到保留门槛，决定淘汰且不接入Agent；独立集未读取。
- 2026-07-18纠正预测态真值泄漏：新增 `prediction_kg_bundle_v2` 无真值快照、`agent_forecast_report_v2`、独立 Forecast Agent 运行时和六项边界测试；默认 `run_agent_report.ps1` 已切换为预测态，旧 `agent_report_v1` 仅由显式 Verification 入口运行。
- 2026-07-18新增版本化内部一致性规则接口：候选规则最初输出`possible_underestimation/watch`供评估，不能改写冻结Risk JSON；后续已按development结果降级为diagnostic_only。静态知识接入前先验返回`not_available`，接入后为`context_only`且风险为空。
- 2026-07-18完成一次真实 Luna 无真值暴雨联调：报告和预测快照均由Schema强制声明 `truth_accessed=false`，未读取同期洪灾、伤亡、新闻或事后验证；加入静态知识、细空间诊断和development审计后完整回归为139项通过。在线调用成功不代表天气预测准确率已经验证。
- 2026-07-18补齐完整链路的事后验证Agent层：保留冻结Risk JSON—综合评估图谱SHA闭合证据包、五个只读函数工具、`agent_report_v1`结构化Schema、风险事实与来源Guardrail、确定性回退，以及Luna默认/Terra单次升级的可选OpenAI Agents SDK运行时。该链路现在明确标为结果锁定后的 Verification，不能作为预测报告。
- 2026-07-18新增零前端依赖的本地可视化研判台：从13区边界、33份冻结暴雨Risk JSON和版本化评估清单确定性生成页面数据；提供独立暴雨命中、Jazan已知漏报、规则证据链和验证边界视图。页面明确标注高温仍为development blocked、影响层无已审查负样本、Neo4j仅为本地联调，不修改冻结规则或GraphCast → MAZU-like流程。
- 2026-07-18为研判台增加无第三方前端依赖的答辩材料导出：当前案例可生成1600×900 PNG证据卡和Markdown摘要；导出保留数据分区、规则状态、漏报归因及非官方预警声明，不重新计算风险。
- 2026-07-18完成高温development误差诊断：24条留一案例配对显示偏差订正将MAE从2.093°C降至0.746°C；将4个事件目标窗未命中分别归因于聚合代理差异、预报/订正不足、连续日门槛和事件标签错位。新增P95/最大值混合敏感性分析，但明确其为看过development后的回顾性探索，不冻结规则、不打开独立高温集。
- 2026-07-18在读取新增预报前完成高温v3前瞻development锁：仅凭SSOD选择SA-08的`20200623_00`对照与`20200702_00`事件，固定回顾性候选权重0.6和既有偏差订正；记录输入SHA、14天独立禁运缓冲和2/2事件、2/2对照门槛。锁定时本地无对应GraphCast/MAZU-like产物，独立高温继续封存。
- 2026-07-18在预注册提交`b427e36`后完成两个前瞻案例的24/24缓存与6/6 NetCDF交付，主交付扩展为63/63、ADM1摘要9009行、development配对225/225 accepted。固定权重0.6候选在SA-08新对照上2/2正确否定、在新事件上0/2命中，未通过前瞻门槛；未搜索替代权重或降低阈值，高温继续blocked，独立高温继续封存。

- 2026-07-18在读取新增GraphCast预报前，使用SSOD观测锁定高温第二轮development扩展：`20200622_00`对照和`20200627_00`事件；新增选择、批处理catalog、SHA锁、自动验收与说明文档，独立高温继续封存且规则未修改。
- 预注册提交推送后完成第二轮A侧交付：新增24个本地GraphCast缓存、6份MAZU-like NetCDF，主交付扩展为57/57 passed、8151行ADM1摘要；development配对扩展为189/189 accepted（IMERG 45、SSOD 144）。
- 新增`heatwave_bias_correction_cv_v2.yaml`锁定扩展输入；估计器、阈值、门槛和修正上限继承v1不变。锁定时新配对已生成但尚未计算扩展样本汇总CV，保留该时序限制。
- 输入锁推送后完成8折高温第二轮CV：固定修正`+2.306°C`，目标窗召回5/9、事件案例检出3/5、对照6/6正确拒绝；前两项仍未达门槛，结论保持blocked，独立高温未开启。
- 新增 `2026-07-15_B与C协助开发进展.md`，集中记录 A 协助完成的 B/C 开发产物、仓库证据、43 项回归结果、未完成边界和后续责任，避免把工程骨架误写为本人验收或业务完成。

- 明确 MAZU 2025 的开发定位与 GraphCast 2020 独立回放边界。
- 补充 B→C 风险 JSON Schema、最小示例、风险引擎、图谱和评估协议。
- 将项目状态分为已完成、接口已完成和未开始，避免将 B/C 计划描述为实现。
- 未修改 GraphCast → MAZU-like 代码、NetCDF 字段或现有正式交接文件。
- 成员 A 协助完成 MAZU 2025 全年 365 份文件的 11 个共享指标审计，新增可复现脚本和覆盖率汇总；风险阈值与规则仍待成员 B。
- 新增可追溯的沙特 ADM1 边界、13 区域注册表及 MAZU 年度/季节描述统计；边界采用 geoBoundaries ODbL 数据，后续已补充政府来源的阿拉伯语名称与图谱开发骨架。
- 将三个现有 GraphCast MAZU-like lead 按相同 ADM1 边界生成区域指标摘要，供 B 开发 Risk JSON；未生成任何风险等级。
- 成员 A 协助 B 完成跨源时间语义分级、区域/季节候选阈值、暴雨/高温草案引擎及 78 份证据链试跑；输出隔离在 `handoff/risk_dry_runs/`，规则仍为 `draft`，未宣称冻结或验证完成。
- 新增 Risk JSON 语义验收器、规则临界/缺测/连续高温测试，以及 IMERG/GHCN 标准化、同口径匹配和天气指标计算框架。
- 2026-07-16 扩展候选目录为 7 个事件、5 个对照和 1 个 demo，增加预注册数据集划分；扩展来源/影响证据和候选 Schema，状态仍保持待复核、未冻结。
- 实际下载并校验 GHCN-Daily 2020 年文件，新增流式沙特站点/候选窗口筛选及小型覆盖摘要；IMERG Final Run V07B 已确认可用但尚未认证下载，因此仍无完整真实天气验证结论。
- 新增 IMERG Final Run V07B 最小日文件下载计划和认证下载工具，只覆盖 22 个暴雨候选/对照日期，并提供原子写入、ZIP 内容校验和 SHA-256 清单；凭据不进入仓库。
- 完成 22 个 IMERG V07B 日文件下载和校验，新增官方缩放/缺测处理、读取时沙特裁剪、固定 ADM1 掩膜缓存、逐日与案例窗口聚合；处理时间由低效初版的数分钟降至约 10 秒。
- 4 个暴雨对照的目标区域窗口 P95 与最大值均低于 31 天内匹配事件，候选状态更新为 `imerg_screened_lower_intensity`；该状态只表示相对低强度，不虚构无雨或无灾害影响。
- 完成 A 侧交付加固：新增 NetCDF 契约/QC 验收、catalog 预检查、损坏缓存隔离、临时文件原子交付、SHA-256/实现指纹清单和一键交付脚本；三个现有 lead 已全链路复验通过。
- 成员 A 协助 C 完成独立开发工作：补充 13 区阿拉伯语名称和总部来源、2020 候选事件/待筛选对照、灾害真值 Schema 与首批证据、Neo4j 5.x 约束/导入 bundle/固定查询及确定性报告模板。在该阶段，案例、对照和证据仍待后续推进，正式报告仍依赖冻结 Risk JSON；后续状态见 2026-07-16 阶段记录。
- 逐条复核 12 条灾害影响真值及其网页来源，新增 `disaster_evidence_review.csv` 记录可访问性、日期/地区/陈述匹配、佐证和限制；9 条暴雨影响保持 `yes`，3 条高温影响保持 `unknown`，没有把人工复核等同于官方证据或无影响结论。
- 新增 13 行案例批准建议：12 个真实候选均建议用于天气层，只有 5 个具有已复核影响证据的暴雨事件建议作为影响层正例；清单先保留批准闸门，随后根据成员 A 的明确决定记录最终批准。
- 成员 A 在明确采用单人继续推进方式后确认按建议批准：12 个真实案例改为 `approved`，冻结为 7 个 development 与 5 个 independent_test；天气层批准没有把高温或对照虚构成影响层样本。
- 按 A/B/C 技术归属统一更新进度：A 的正式 GraphCast 批处理已启动；B 名下的 GHCN/IMERG 观测准备与候选筛选、C 名下的案例/证据复核与图谱骨架均注明由 A 代办，避免误写为 B/C 本人验收。
- 新增 `2026-07-16_阶段进展与正式批处理启动.md`，集中记录完成项、进行中任务、剩余依赖和严格后续顺序。
- 2026-07-17完成A侧正式交付收尾：12个案例的144个GraphCast缓存时次、36份正式NetCDF全部完成；39份文件（含demo）均通过验收，ADM1摘要扩展为5577行，delivery manifest扩展为39/39 passed。
- 修正区域摘要回归测试不再硬编码旧的3份demo，而是逐文件验证39份输入及每份13区域×11指标的完整性。
- 开始B侧development天气验证：新增只读取7个development案例的配对生成器、63条预报—观测配对和81条覆盖审计；5个independent_test案例保持封存。
- 明确27条IMERG配对为accepted、36条GHCN配对因观测时刻缺失为provisional，并版本化记录6个缺失IMERG日期对应的18条未配对聚合，未静默丢弃缺口。
- 补齐6个development lead所需IMERG UTC日文件，IMERG本地集合扩展为28日、320.2 MiB；逐文件ZIP/SHA校验通过，development配对由63/81闭合为81/81，当前45条IMERG accepted、36条GHCN provisional、missing=0。
- 冻结development天气诊断QC v1：IMERG以精确UTC日窗和覆盖率不低于0.95进入accepted连续指标；GHCN因`OBS-TIME`缺失继续保持provisional，仅允许非正式连续诊断。
- 新增development专用指标入口和36行小型交接结果：12行IMERG MAE/RMSE/Bias、24行GHCN provisional诊断；阈值与分类字段保持空白，未读取independent_test。
- 新增development专用风险规则复验入口，只使用7个开发案例的目标区域和对应灾种生成21条草案判定；5个independent_test案例继续排除。
- 修正规则复验时间窗口径：21条判定中12条与案例目标窗重叠并计分，9条作为前后上下文保留但不计命中/漏判；v1实际为1命中、5漏判、6正确低风险。
- 新增development-only v2规则和机器可读冻结评估：v2为4命中、2漏判、6正确低风险；暴雨达到预设闸门后单独冻结，高温因样本量与GHCN QC不足保持draft。
- 生成并验收15份冻结暴雨development Risk JSON、15份受控报告及69节点/98关系图谱bundle；所有产物保留development与独立测试未开启声明。
- 独立暴雨评估因缺少2020-07-11、07-25、11-25、11-27四个IMERG UTC日文件暂缓，未生成残缺测试结论。
- 新增独立暴雨IMERG专用4日计划和下载安全闸门：默认仍拒绝independent_test，只有显式指定独立分区并提供frozen暴雨规则时才允许计划/下载；凭据只在UTF-8交互窗口临时存在。
- 补齐并校验4个独立暴雨IMERG日文件，本地集合扩展为32日、385,242,883字节和416行ADM1日摘要。
- 在规则/观测SHA锁下完成一次性独立暴雨评估：54/54配对accepted，P95为6命中、0漏报、0空报、12正确否定；生成18份冻结Risk JSON和18份受控报告，明确禁止回调规则。
- 生成包含15份development与18份independent暴雨结果的综合图谱bundle，共87个节点、152条关系；Neo4j实机仍未声明部署。
- 接入NOAA SSODv2 2020年31个沙特站点年度文件，新增逐文件大小/SHA-256清单、30站ADM1映射和26,268行同步UTC区域日摘要；原始文件继续由Git忽略。
- 按观测先验规则新增2个高温事件与2个对照；SSOD纠正`20200715_00`的旧GHCN对照误判，并用不重叠的`20200908_00`替换重叠的`20200717_00`。
- 正式GraphCast交付扩展为16个批准案例加1个demo：204个缓存时次、51份NetCDF、7293行ADM1摘要和51/51 passed交付清单。
- development扩展为11个案例，形成153/153条accepted配对（IMERG 45、SSOD 108）、36行指标和18行高温时序；独立高温未读取。
- 按预注册门槛复验高温v2：4事件/2对照和观测QC门槛已满足，但事件目标窗召回仅1/7、事件案例检出仅1/4，因此保持`draft`并继续封存独立高温案例。
- 在结果生成前提交并锁定高温冷偏差修正方案，随后完成6折留一案例中位误差交叉验证；固定修正估计为`+2.280°C`，目标窗召回由1/7改善为3/7、事件案例检出由1/4改善为2/4且未引入对照误报，但仍低于预注册门槛，因此结论为`blocked`，未读取独立高温案例、未降低规则阈值。
- 完成Neo4j Community 2026.06.0本地实机联调，综合暴雨bundle精确导入为87节点、152关系和6约束，三组固定查询均通过；实机发现并修复旧`MERGE`折叠4条平行影响证据的问题，新增关系身份测试、在线精确验收器和一键幂等复验脚本。
- 完成灾害影响层描述性正例覆盖评估：9条reviewed yes记录合并为6个案例—区域单位，冻结中高风险窗口覆盖5/6；`20200501_00 / SA-09`为唯一未覆盖单位。当前无reviewed no记录，负类指标保持空值，天气层独立分区不被虚构为影响层盲测。
- 完成唯一影响漏报的分窗归因：lead024为高置信度天气预报低估，lead048为中置信度规则空间尺度盲区并伴随天气低估；冻结暴雨规则未修改。检索4个暴雨对照的官方/民防/媒体结果，未发现合格过程后无影响陈述，全部保持`unknown`。
