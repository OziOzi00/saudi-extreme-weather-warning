# 2026-07-15 成员 B、C 协助开发进展

> 本文是 2026-07-15 当日快照。后续案例、真实观测、证据复核、批准决定和批处理状态请以[2026-07-16 阶段进展与正式批处理启动](2026-07-16_阶段进展与正式批处理启动.md)为准。

本文记录 2026-07-15 由成员 A 协助推进的 B、C 工作。所有“完成”均指仓库中可以复现和测试的开发产物；不代表成员 B/C 已亲自验收，也不代表规则、案例、图谱服务或预警效果已经正式完成。

## 今日结论

| 范围 | 当前结论 | 验收边界 |
| --- | --- | --- |
| B 可独立完成的工程开发 | 基本完成 | 草案规则、风险引擎、验收器和天气验证骨架可运行 |
| B 正式业务任务 | 未完成 | 缺真实 IMERG/GHCN、规则冻结、正式 Risk JSON 和效果结论 |
| C 可独立完成的工程开发 | 基本完成 | 候选案例、真值结构、图谱 bundle/导入器/查询和报告模板可运行 |
| C 正式业务任务 | 未完成 | 缺证据最终复核、批准案例、Neo4j 实机联调和正式 Risk JSON |
| 全仓库回归 | 通过 | `43 passed`；不等同于天气或灾害预测效果通过 |

## 成员 B：今日已完成的开发产物

1. 完成 MAZU 2025 的 11 个共享指标覆盖审计、ADM1 年度/季节描述统计和 GraphCast 演示 lead 区域摘要。
2. 明确 MAZU 与 GraphCast 的时间语义差异、可比级别和聚合口径，避免把代表时次误写为日平均或日极值。
3. 形成强降水、高温/热浪候选规则 YAML；规则状态保持 `draft`。
4. 实现草案风险引擎并生成 78 份逐区域、灾种和 lead 的开发试跑结果，统一隔离在 `handoff/risk_dry_runs/`。
5. 实现 Risk JSON 的结构、时间、区域、证据链、重复身份和正式冻结状态验收。
6. 实现 IMERG/GHCN 标准化、观测配对和天气指标计算骨架，并以合成数据覆盖关键边界测试。
7. 提供 `scripts/run_b_dry_integration.ps1`，用于复现 B 侧草案联调。

### B 的仓库证据

- `manifests/mazu_2025_indicator_coverage.csv`
- `handoff/mazu_statistics/`
- `handoff/region_summaries/`
- `docs/指标时间语义与聚合口径.md`
- `docs/候选规则统计解释.md`
- `configs/heavy_rain_rules_v1.yaml`
- `configs/heatwave_rules_v1.yaml`
- `src/saudi_warning/risk/`
- `src/saudi_warning/verification/`
- `handoff/risk_dry_runs/`

## 成员 B：之后必须完成

| 任务 | 依赖 | 完成判据 |
| --- | --- | --- |
| 本人复核 A 协助的统计、规则和代码 | B | 对候选阈值、缺测和区域差异给出可追溯评审意见 |
| 取得真实 IMERG/GHCN | C/全队先确认案例 | 记录版本、URL、日期、散列、许可和有效覆盖 |
| 确定观测 QC | 真实观测 | 明确最低覆盖率、最低站点数和时间不一致处理 |
| 完成天气层验证 | A 的正式 case 输出 | 产生降水/温度误差、命中漏报空报等结果，不隐藏缺测 |
| 冻结风险规则 | B 提案、全队评审 | YAML 明确 `frozen` 版本、依据、日期和兼容性 |
| 输出正式 Risk JSON | 冻结规则与正式 NetCDF | 写入 `handoff/risk_results/` 并通过 `require_frozen` 验收 |

## 成员 C：今日已完成的开发产物

1. 为 13 个统一 `SA-xx` 区域补充阿拉伯语名称、行政中心和政府来源链接；几何边界继续如实标注为 geoBoundaries 2017 参考边界。
2. 建立候选案例清单：两个 2020 事件、两个待 B 观测筛选的候选对照和一个既有 demo。
3. 建立候选案例与灾害影响真值 Schema、字段模板、来源目录和首批逐证据记录；资料不足严格标记为 `unknown`。
4. 实现跨文件 ID、时间、状态和证据引用校验，防止把未经筛选的对照日标为正常天气。
5. 实现数据库无关的 `kg_bundle_v1`、Neo4j 5.x 唯一约束、参数化导入器和三类固定查询。
6. 生成当前开发 bundle：32 个节点、27 条关系；只包含摘要和来源，不嵌入气象栅格或网页全文。
7. 实现确定性报告模板，原样转述 Risk JSON；正式模式强制拒绝 `draft/example` 结果。
8. 提供 `scripts/run_c_development.ps1`，用于复现校验、bundle、示例报告和 C 侧测试。

### C 的仓库证据

- `configs/region_registry.csv`
- `configs/case_catalog_candidates.csv`
- `schemas/candidate_case.schema.json`
- `schemas/disaster_impact_record.schema.json`
- `handoff/disaster_truth/`
- `src/saudi_warning/knowledge_graph/`
- `neo4j/`
- `handoff/knowledge_graph/`
- `src/saudi_warning/reporting/`
- `handoff/reports/`

## 成员 C：之后必须完成

| 任务 | 依赖 | 完成判据 |
| --- | --- | --- |
| 复核首批灾害证据 | C | 增加官方原文或第二独立来源，将可靠记录改为 `reviewed` |
| 确认候选对照日 | B 的 IMERG/GHCN 筛选 | 排除异常天气后由全队批准，不能仅凭未发现新闻确认 |
| 批准正式案例与起报时间 | 全队 | 明确事件、对照、区域、lead 和开发/独立测试划分 |
| 复核边界方案 | C/全队 | 决定继续使用 2017 参考边界或采用许可清晰的新边界 |
| Neo4j 实机联调 | 本地 Neo4j Community | 约束、bundle 导入和固定查询在实际服务中通过 |
| 生成正式图谱与报告 | B 的冻结 Risk JSON | 正式 bundle/报告保留来源、验证状态和证据边界 |
| 完成影响层验证 | 正式案例和灾害真值 | 输出命中、漏报、空报及原因，`unknown` 不计作 `no` |

## 下一阶段依赖顺序

```text
C/全队确认候选案例
  → A 生成正式 24/48/72h MAZU-like 数据与交付清单
  → B 获取真实观测、验证并冻结规则
  → B 输出正式 Risk JSON
  → C 实机导入 Neo4j、生成正式报告并完成影响层验证
```

## 本次验收记录

```powershell
$env:PYTHONPATH = "$PWD\src"
python -m pytest -q
# 43 passed

powershell -ExecutionPolicy Bypass -File scripts/run_c_development.ps1
# 32 nodes, 27 relations; 5 C-side tests passed
```

同时完成 JSON Schema 解析、Markdown 本地链接、Python 编译、源码行长和 `git diff --check` 检查。Neo4j 实际服务和真实 IMERG/GHCN 未在本次验收中运行，不能把代码测试写成业务效果验证。
