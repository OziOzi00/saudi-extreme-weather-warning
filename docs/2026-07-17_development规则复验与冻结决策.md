# 2026-07-17 development规则复验与冻结决策

## 结论

最终采用灾种级决策：暴雨v2已冻结，高温v2继续保持`draft`。暴雨已生成development正式Risk JSON；高温没有进入正式目录。5个`independent_test`案例没有参与任何v2参数选择，高温独立案例仍未打开。

首次复验使用7个development案例。随后在不读取独立测试的前提下，引入同步`00–23 UTC`的NOAA SSODv2观测，扩展为11个development案例。最终复验只使用这些案例的目标区域、对应灾种和24/48/72h窗口：20条目标窗口参与评分，13条事件前后上下文只保留、不计命中或漏判。

## 复验结果

| 规则 | 灾种 | 目标窗口 | 候选命中 | 候选漏判 | 正确低风险 | 决策 |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| v1 | 暴雨 | 9 | 1 | 4 | 4 | 不冻结 |
| v1 | 高温 | 11 | 0 | 7 | 4 | 不冻结 |
| v2 | 暴雨 | 9 | 3 | 2 | 4 | 通过预设闸门，冻结 |
| v2 | 高温 | 11 | 1 | 6 | 4 | 召回和事件案例检出未达闸门，保持draft |

暴雨v2的目标窗口召回为0.60、特异度为1.00，3个事件案例中2个至少有一个目标窗口被识别，2个对照案例全部保持低风险。门槛改为中等级`max(季节P50, 5 mm)`、高等级`max(季节P90, 10 mm)`，保留辅助证据要求和medium置信度上限。

这些是候选规则审查标签，不是正式命中率、漏报率或影响层真值。对照案例只被确认“相对低强度”，并没有可靠证据证明完全无灾害影响。

同站点`station_max`连续指标显示TMAX MAE为2.053°C、RMSE为2.201°C、Bias为-2.053°C，当前18个lead配对中预测值均偏低。这支持“系统性冷偏差”作为后续调查假设，但不能据此直接下调冻结阈值。随后已在结果产生前预注册留一案例中位误差修正，并锁定输入SHA；复验把目标窗召回由1/7改善到3/7、事件案例检出由1/4改善到2/4，但仍未达到0.60和0.666667门槛，因此结论仍为阻塞。

## 灾种级冻结理由

1. 暴雨有3个事件、2个对照，IMERG配对全部为accepted，并达到规则文件预先记录的最低召回、特异度、事件案例覆盖和对照拒绝闸门，因此冻结为受限原型v2。
2. 冻结不等于独立效果已验证。正式development JSON的`verification`明确指向连续指标和规则复验，并记录独立测试未开启。
3. 高温development现有4个事件、2个对照，108条SSOD配对均为`accepted`，样本量、观测QC、特异度和对照拒绝闸门均满足。但7个事件目标窗只命中1个，召回率为0.142857；4个事件案例只检出1个，案例检出率为0.25，低于预注册的0.60和0.666667，因此不得冻结。
4. SSOD同步观测还推翻了旧`20200715_00`的GHCN对照标签；该案例已改为事件。与其重叠的`20200717_00`在评估前撤下，并换成不重叠的`20200908_00`，避免重复热过程夸大样本。
5. 不能降低高温门槛来追逐本次development结果，也不能使用独立高温案例弥补召回不足；已经冻结的暴雨v2同样不得根据独立结果回调。

## 后续闸门

1. 4个缺失IMERG日文件已补齐，独立暴雨评估已一次性执行并锁定；结果不得用于回调v2。
2. 高温独立案例继续封存。预注册的6折留一案例冷偏差修正已经完成且未通过；下一步需要新增不参与本轮方法选择的development证据，或在看到新结果前预注册有物理依据的新候选方法，不得对同一结果反复试算法或降低冻结规则阈值。
3. Neo4j图谱bundle和报告已生成，但实机服务尚未验证，不能声明数据库已部署。

机器可读闸门状态见`manifests/rule_freeze_readiness.csv`，原始v2定量评估见`manifests/development_v2_freeze_assessment.csv`，偏差修正交叉验证见`manifests/heatwave_bias_cv_assessment.csv`。
